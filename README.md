# Edzy Sitemap Crawler

## Setup
1. `npm install`
2. Create `.env` with `MONGO_URI` and `PORT`.
3. `npm run dev` (This starts the crawler automatically).

## Testing
Import the provided `Bruno_Collection.json` into the Bruno app to test the 3 POST endpoints.